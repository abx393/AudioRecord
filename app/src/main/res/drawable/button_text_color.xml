<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- selected and not pressed -->
    <item android:state_enabled="true"
        android:color="#fff" />
    <item android:state_enabled="false"
        android:color="@color/grey" />

</selector>
